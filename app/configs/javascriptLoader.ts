import { Configuration } from 'webpack'
export const javascriptLoader = () => ({
    module: {
        rules: [
            {
                test: /\.tsx?$/,
                use: 'ts-loader',
                exclude: /node_modules/,
            },
        ],
    },
})
